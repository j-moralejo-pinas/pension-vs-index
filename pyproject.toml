[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "pension-vs-index"
description = "After-tax comparison of company pension fund investing vs equivalent index fund investing under Spanish tax rules."
dynamic = ["version"]
authors = [
    { name = "Javier Moralejo Piñas", email = "j.moralejo.pinas@gmail.com" },
]
readme = "README.rst"
license = { text = "MIT" }
keywords = ["finance,simulation,financial-simulation,investment-simulation,pension-fund,index-fund,retirement-planning"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.14"
dependencies = [
    # Add your project dependencies here
]

[project.urls]
Homepage = "https://github.com/j-moralejo-pinas/pension-vs-index"
Changelog = "https://pension-vs-index.readthedocs.io/en/latest/CHANGELOG.html"
Source = "https://github.com/j-moralejo-pinas/pension-vs-index"
Documentation = "https://pension-vs-index.readthedocs.io/"
Tracker = "https://github.com/j-moralejo-pinas/pension-vs-index/issues"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build]
include = []

[tool.hatch.build.targets.wheel]
packages = ["src/pension_vs_index"]
exclude = ["/tests"]

[project.optional-dependencies]
dev = [
    "pytest",
    "pyright[nodejs]",
    "pre-commit",
    "ruff",
    "pydoclint",
    "docformatter",
    "pytest",
    "pytest-cov",
    "pyupgrade",
]
docs = ["sphinx", "sphinx-autoapi", "sphinx-linkfix", "sphinx-rtd-theme"]
format = ["ruff", "docformatter", "pyupgrade"]
code-quality = ["ruff", "pydoclint", "pyright[nodejs]"]
test = ["pytest", "pytest-cov"]

[tool.pytest.ini_options]
pythonpath = ["src", "tests"]
addopts = "-ra --strict-markers"
# norecursedirs = ["build", "dist"]
testpaths = ["tests"]

[tool.coverage.run]
source = ["src"]
omit = []

[tool.ruff]
fix = true
unsafe-fixes = true
line-length = 100

[tool.ruff.format]
docstring-code-format = true
docstring-code-line-length = 100

[tool.ruff.lint]
ignore = [
    "S101",    # Use of `assert` for testing purposes
    "COM812",  # Add commas at the end of an iterable
    "S311",    # Standard pseudo-random generators are not suitable for cryptographic purposes
    "TD003",   # Missing issue link for this TODO
    "FIX001",  # Line contains FIXME, consider resolving the issue
    "FIX002",  # Line contains TODO, consider resolving the issue
    "PLR0913", # Too many arguments in function definition
    "PLR0915", # Too many statements
    "ANN401",  # Dynamically typed expressions (typing.Any) are disallowed in `**kwargs`
]
select = ["ALL"]
exclude = ["docs/*"] # "tests/*"

[tool.ruff.lint.pycodestyle]
max-doc-length = 100

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ruff.lint.per-file-ignores]
"tests/*" = [
    "SLF001",  # Private member accessed
    "PLR2004", # Magic value used in comparison
    "INP001",  # File is part of an implicit namespace package.
]
"src/pension_vs_index/_version.py" = ["ALL"]

[tool.pydoclint]
style = 'numpy'

[tool.docformatter]
style = 'numpy'
wrap-summaries = 100
wrap-descriptions = 100
pre-summary-newline = true
close-quotes-on-newline = true

[tool.vulture]
# min_confidence = 80
paths = ["src", "tests"]

[tool.bandit]
skips = ["B101"]

[tool.cicd]
workflow = "trunk"
