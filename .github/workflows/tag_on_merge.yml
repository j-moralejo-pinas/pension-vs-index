name: tag_on_merge

on:
  pull_request:
    types: [closed]
    branches: [main]

permissions:
  contents: write

jobs:
  call-reusable:
    uses: j-moralejo-pinas/ci-cd-python/.github/workflows/tag_on_merge.yml@367643d5a90bab6a2df1fff6671a25b548501527
    secrets: inherit
