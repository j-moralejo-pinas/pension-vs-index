name: main_push

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

jobs:
  call-reusable:
    if: github.event.before != '0000000000000000000000000000000000000000'
    uses: j-moralejo-pinas/ci-cd-python/.github/workflows/main_push.yml@367643d5a90bab6a2df1fff6671a25b548501527
    secrets: inherit
